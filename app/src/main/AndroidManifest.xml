<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ppemonoyer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PPEMonoyer">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.ProfileActivity"
            android:label="@string/toolbar_title_login_activity"
            android:parentActivityName=".ui.MainActivity"/>

        <activity android:name=".ui.ParametersActivity"
            android:label="@string/toolbar_title_parameters_activity"
            android:parentActivityName=".ui.MainActivity"/>

        <activity android:name=".ui.LectureActivity"
            android:label="@string/toolbar_title_test_activity"
            android:parentActivityName=".ui.MainActivity"/>

        <activity android:name=".ui.DebutFin"
            android:label="@string/toolbar_title_début_fin_activity"
            android:parentActivityName=".ui.MainActivity"/>

    </application>

</manifest>